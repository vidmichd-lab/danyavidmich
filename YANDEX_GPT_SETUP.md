# 🚀 Быстрая настройка Yandex GPT

## Шаг 1: Создайте каталог в Yandex Cloud

1. Зайдите в [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Выберите или создайте облако
3. Создайте новый **каталог** (folder) или используйте существующий
4. Скопируйте **Folder ID** из URL или настроек каталога
   - URL выглядит так: `https://console.cloud.yandex.ru/folders/b1g...`
   - Folder ID: `b1g...` (после `/folders/`)

## Шаг 2: Создайте сервисный аккаунт

1. В каталоге перейдите в **Сервисные аккаунты** → **Создать аккаунт**
2. Укажите имя (например, `ai-content-generator`)
3. Нажмите **Создать**

## Шаг 3: Назначьте роль

1. Откройте созданный сервисный аккаунт
2. Перейдите на вкладку **Роли**
3. Нажмите **Назначить роли**
4. Выберите роль: **AI Language Models User** (`ai.languageModels.user`)
5. Сохраните

## Шаг 4: Создайте API ключ

1. В сервисном аккаунте перейдите на вкладку **Ключи**
2. Нажмите **Создать ключ** → **API ключ**
3. Скопируйте ключ (показывается только один раз!)

## Шаг 5: Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
YANDEX_API_KEY=AQVN...ваш-ключ
YANDEX_FOLDER_ID=b1g...ваш-folder-id
```

Или экспортируйте в терминале:

```bash
export YANDEX_API_KEY="AQVN...ваш-ключ"
export YANDEX_FOLDER_ID="b1g...ваш-folder-id"
```

## Шаг 6: Сгенерируйте контент

```bash
npm run ai:generate
```

Система автоматически:
- Определит, что используется Yandex GPT
- Сгенерирует контент для всех проектов
- Сохранит в `src/data/ai-content.json`
- Создаст кеш для быстрого доступа

## ✅ Проверка

После генерации проверьте:
- Файл `src/data/ai-content.json` содержит сгенерированный контент
- В логах видно `Generating content for ... using yandex...`

## 💡 Полезные ссылки

- [Документация Yandex GPT](https://yandex.cloud/ru/docs/foundation-models/concepts/)
- [Цены на Yandex GPT](https://yandex.cloud/ru/prices#foundation-models)
- [Лимиты и квоты](https://yandex.cloud/ru/docs/foundation-models/quotas-and-limits)

## 🔒 Безопасность

⚠️ **Важно:** Не коммитьте `.env` файл в Git! Он уже добавлен в `.gitignore`.

Для GitHub Actions добавьте секреты:
- `YANDEX_API_KEY`
- `YANDEX_FOLDER_ID`

