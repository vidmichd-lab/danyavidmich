---
import BaseLayout from "@layouts/BaseLayout.astro";
import Header from "@components/Header.astro";
import { getOGMeta } from "@utils/ogMeta";
import { getOptimizedImagePath } from "@utils/imageUtils";

const ogMeta = await getOGMeta("s7", `Danya Vidmich — S7 Logistics Identity`);
const subtitle = "S7 Logistics Identity";
const tertiary = "";
---

<BaseLayout
  title={ogMeta.title}
  description={ogMeta.description}
  ogTitle={ogMeta.ogTitle}
  ogDescription={ogMeta.ogDescription}
  ogUrl={ogMeta.ogUrl}
  ogImage={ogMeta.ogImage}
>
  <Header subtitle={subtitle} tertiary={tertiary} />


        <div class="main">
      		<div class="about9">

      		    <div>

      		        <p>
      		                Develop a sub-brand for S7’s B2B delivery service that communicates the ability to ship packages by air using the airline's technology, ensuring delivery within 24 hours.

  I based the visual language on a metaphor of clouds of different sizes, symbolizing packages of any dimension. These “clouds” fly through real clouds, reinforcing the concept of flight and seamless delivery.

  By maintaining S7’s recognizable color scheme, I created a bold and distinctive design system that stands out among other shipments, ensuring strong brand visibility.</p>
  <div class="button">
      			        <span class=".solar--play-circle-bold"></span><a href="https://logistics.s7.ru/">Open S7 Logistics</a>
      			    </div>
      			    </div>  
          </div>

          <div class="aboutzero"></div>

      		<div id="page3" class="graphic">

      			<div id="bigcard2" class="bigcard">
      				<img src={getOptimizedImagePath("/img/gr/s7/1.webp")} loading="lazy" decoding="async" />
      			</div>


            <div id="bigcard2" class="bigcard">
              <img src={getOptimizedImagePath("/img/gr/s7/2.webp")} loading="lazy" decoding="async" />
            </div>

            <div id="bigcard2" class="bigcard">
              <img src={getOptimizedImagePath("/img/gr/s7/3.webp")} loading="lazy" decoding="async" />
            </div>

      		</div>
      	</div>
</BaseLayout>
