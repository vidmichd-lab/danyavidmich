# AI Content Integration

Система для автоматической генерации единообразных текстов для всех проектов с помощью ИИ.

## 🎯 Возможности

- **Единый стиль**: Все тексты генерируются по единому стиль-гайду
- **Автоматическое заполнение**: Описания для проектов генерируются автоматически
- **Кеширование**: Результаты кешируются для быстрой работы
- **Гибкость**: Можно использовать AI или fallback на структурированный контент

## 📦 Установка

1. Установите зависимости (если нужно):
```bash
npm install
```

2. Установите API ключ OpenAI:
```bash
export OPENAI_API_KEY="your-api-key-here"
```

Или создайте файл `.env`:
```
OPENAI_API_KEY=your-api-key-here
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini
```

## 🚀 Использование

### Генерация контента для всех проектов

```bash
npm run ai:generate
```

Скрипт:
- Читает все проекты из `src/data/cases.json`
- Генерирует описания через OpenAI API
- Сохраняет результаты в `src/data/ai-content.json`
- Кеширует каждый проект в `.ai-cache/`

### Использование в коде

AI-контент автоматически используется в:
- **OG мета-тегах** (`ogMeta.ts`) - для соцсетей
- **Описаниях проектов** - можно использовать на страницах

Пример:
```typescript
import { getProjectAIContent } from "@utils/aiContentStore";

const aiContent = getProjectAIContent("apt-product");
// aiContent.shortDescription - для карточек
// aiContent.longDescription - для страниц проектов
// aiContent.subtitle - для заголовков
```

## 📝 Стиль-гайд

Все тексты генерируются по единому стиль-гайду:

- Активный залог
- Конкретные достижения и результаты
- Упоминание технологий/инструментов
- Краткость (50-80 слов для коротких, 100-150 для длинных)
- Фокус на дизайн-решениях и результатах
- Профессиональная терминология
- Избегание общих фраз
- Фактичность и ориентация на результаты

## 🔧 Конфигурация

### Переменные окружения

- `OPENAI_API_KEY` - API ключ OpenAI (обязательно)
- `AI_PROVIDER` - провайдер (`openai`, `anthropic`, `local`, `none`)
- `AI_MODEL` - модель (по умолчанию `gpt-4o-mini`)

### Настройка промптов

Промпты можно изменить в:
- `src/utils/aiContent.ts` - стиль-гайд и шаблоны
- `scripts/generate-ai-content.mjs` - промпты для генерации

## 💾 Кеширование

- Результаты кешируются в `.ai-cache/` (по одному файлу на проект)
- Финальный результат в `src/data/ai-content.json`
- Кеш можно удалить для перегенерации: `rm -rf .ai-cache/`

## 🔄 Workflow

1. **Добавление нового проекта**:
   - Добавьте проект в `src/data/cases.json`
   - Запустите `npm run ai:generate`
   - AI сгенерирует описания автоматически

2. **Обновление существующих**:
   - Удалите кеш для проекта: `rm .ai-cache/project-id.json`
   - Запустите `npm run ai:generate`

3. **Использование без AI**:
   - Если API ключ не установлен, используется fallback
   - Генерируется структурированный контент на основе данных проекта

## 💰 Стоимость

- `gpt-4o-mini`: ~$0.15 за 1M input tokens, ~$0.60 за 1M output tokens
- Для ~30 проектов: примерно $0.10-0.30 за полную генерацию
- Кеширование снижает повторные запросы

## 🎨 Примеры использования

### На странице проекта

```astro
---
import { getProjectAIContent } from "@utils/aiContentStore";
import { getCases } from "@utils/casesStore";

const cases = await getCases();
const project = cases.find(c => c.slug === "apt-product");
const aiContent = project ? getProjectAIContent(project.id) : null;
---

{aiContent?.longDescription && (
  <p>{aiContent.longDescription}</p>
)}
```

### В OG тегах

OG теги автоматически используют AI-контент через `ogMeta.ts`.

## 🔐 Безопасность

- API ключи хранятся в переменных окружения
- `.env` файлы в `.gitignore`
- Кеш не содержит чувствительных данных

## 📚 Дополнительно

- Можно добавить поддержку других провайдеров (Anthropic, локальные модели)
- Можно настроить разные стили для разных типов проектов
- Можно добавить валидацию и проверку качества контента

