# Performance & Quality Improvements

## ✅ Implemented

### 1. Preconnect для внешних ресурсов
- Добавлены `preconnect` теги для Vimeo, Google Tag Manager и Yandex Metrika
- Улучшает время загрузки внешних скриптов

### 2. Service Worker для offline поддержки
- Файл: `public/sw.js`
- Кэширует статические ресурсы, изображения и HTML страницы
- Автоматически регистрируется при загрузке страницы
- Поддерживает offline режим

### 3. Content Security Policy (CSP)
- Добавлен в `web.config`
- Защищает от XSS атак
- Разрешает только необходимые внешние ресурсы

### 4. Error Tracking (Sentry)
- Утилита: `src/utils/sentry.ts`
- Автоматически инициализируется в production
- Требует переменную окружения `PUBLIC_SENTRY_DSN`
- Фильтрует незначительные ошибки (network errors)
- Включает session replay для отладки

### 5. Тесты в CI/CD
- Unit тесты: `tests/basic.test.ts` (Vitest)
- E2E тесты: `tests/e2e.spec.ts` (Playwright)
- Автоматически запускаются перед деплоем
- Проверяют:
  - Корректность сборки
  - Наличие ключевых файлов
  - SEO мета-теги
  - Работу фильтров
  - Accessibility

## 🔧 Настройка

### Sentry
1. Создайте проект на [sentry.io](https://sentry.io)
2. Получите DSN
3. Добавьте в `.env`:
   ```
   PUBLIC_SENTRY_DSN=https://your-dsn@sentry.io/project-id
   ```
4. Добавьте в GitHub Secrets для production

### Service Worker
- Автоматически работает после деплоя
- Версия кэша обновляется автоматически при изменениях

### Тесты
```bash
# Запуск unit тестов
npm run test:unit

# Запуск E2E тестов (требует сборки)
npm run test:visual

# Все тесты
npm run test
```

## 📊 Результаты

- **Производительность**: Preconnect ускоряет загрузку внешних ресурсов
- **Offline**: Service Worker обеспечивает работу без интернета
- **Безопасность**: CSP защищает от XSS
- **Мониторинг**: Sentry отслеживает ошибки в production
- **Качество**: Автоматические тесты предотвращают регрессии

